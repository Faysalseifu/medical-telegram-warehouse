"""Project source package.

Historically, files under `src/` were executed as standalone scripts.
Adding this package marker enables importing shared utilities (e.g. explainability)
from tests and the dashboard without changing the existing script entrypoints.
"""
